# Vendored browser dependencies

- Vue `3.5.18`: `vue.global.prod.js`
- Apache ECharts `5.5.0`: `echarts.min.js`

These exact browser builds are served from the same origin so the family site does not depend on third-party CDNs at runtime.
